apply plugin: "java"

dependencies {
    compile project(':dhcoder.support')
    compile 'junit:junit:4.+'
    compile 'org.hamcrest:hamcrest-core:1.3+'
}

sourceCompatibility = 1.6
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets.main.java.srcDirs = ["src/"]
